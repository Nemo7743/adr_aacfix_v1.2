# adr_aacfix 技術說明

## 1. 症狀與結論
- 遊戲：使用 NGE／AVG MAKER PORTABLE 引擎（模組名 `NGE APP`，使用者模式 PRX）的 PSP 自製遊戲（初音系列三款）。
- 現象：Adrenaline v8.0.1 上，進入標題前崩潰，錯誤碼 `C2-12828-1`；之後 Vita 無法啟動任何遊戲。
- 根本原因：Vita 3.65 的 ScePspemu（Sony 官方 PSP 模擬器）在處理 **sceAudiocodec 類型 `0x1003`（AAC）** 的 Media Engine 指令 `0x93` 時，對 `ScePspemuConvertArg` 回傳的 NULL 沒有檢查就寫入，造成 `ScePspemuRemoteMe` 執行緒 data abort（故障位址 `0x00000003`）。
- 觸發條件：PSP 端傳來的 arg2 是 **ME 的 EDRAM 位址**（`0x0018B8C0`），而 `ConvertArg` 只認得 PSP 主 RAM。
- 遊戲端：PMP 影片音軌是 AAC-LC（44.1kHz，1024 樣本／frame）；遊戲的 PMP 播放器（`codec_open` 依格式分派 `0x1001/0x1002/0x1003`）在開場 logo 影片時呼叫 `sceAudiocodecInit(0x1003)`。

## 2. 證據鏈
| 來源 | 內容 |
|---|---|
| 崩潰傾印（spsp2dmp，已解密） | 崩潰執行緒 `ScePspemuRemoteMe`，stop reason `0x30004`（data abort），text 內偏移 `+0x1AEEE`（兩次傾印一致，載入基底每次不同） |
| 反組譯（3.65 `eboot.bin.elf`） | `+0x1AEEE: strb r2,[r3],#1`，暫存器 r4=0、r3=3 → 寫入位址 `0x3`；呼叫路徑 Me 主迴圈 `tbh` → `+0xECDC`（指令 `0x90–0x97`）→ 指令 `0x93` → `bl +0x6404` → `bl +0x1AEDC` |
| 插件 log（實機） | 9 次 AAC 初始化，引數皆為 a1=`0xAC44`、a2=`0x0018B8C0`、a3=`0x17700`、a4=`0x10000`、a5=`0xAC44`，與傾印暫存器逐一吻合 |
| 對照組 | 正常 PSP 遊戲、AT3+（gameboot）、MP3 群組：所有 log 中無位址轉換 NULL；它們把 EDRAM 位址當 handle 直接用，不轉換 |
| Adrenaline 原始碼 | 用 `taiInjectData` 把 `+0x6394/+0x6434/+0x6534` 的 `cmp …,#0x1C00000` 改成 `#0x3C00000`（使用者位址窗口 `[0x08400000, 0x0C000000)`）；未處理 ME EDRAM |

## 3. 修正
Hook 三個函式（segment 0，Thumb）：

| 偏移 | 函式 | 作用 |
|---|---|---|
| `+0x6404` | `ConvertArg(addr, mode, size)` | 回傳 NULL 且位址在 `[0x00100000, 0x00200000)` 時，改回傳 1 MiB 影子緩衝區內的位址（一個 ME 位址對應一個固定 host 位址） |
| `+0xECDC` | AAC 指令群組 `group(cmd, a1..a5)` | 預檢會被解參考卻沒有 NULL 檢查的引數槽（`0x90`：a1..a5；`0x91`：a1、a2；`0x93/0x95`：a2；`0x97`：a4）；不可轉換 → 回傳 `-1` |
| `+0x1AEDC` | 崩潰函式 | `ptr == NULL` → 回傳 `-1`（第二道防線） |

為什麼用逐位址影子，不用單一 dummy buffer：AAC 群組的 slot 表（`+0x19DC0/+0x19E14/+0x19E78`）以**指標值**為 key；共用同一塊 dummy 會讓不同 EDRAM 位址互相碰撞。

回傳 `-1` 的依據：同一段程式碼對 `arg2 == 0` 的既有處理是 `mov.w r0,#-1`，主迴圈把 `r0` 以 `vdup.32` ＋ `vshr.s64 #32` 展開成 64 位元回應（`-1` → `0xFFFFFFFFFFFFFFFF`）。預檢在呼叫原函式**之前**就返回，不寫任何記憶體，也不會呼叫 `+0x19DC0`（該函式對 NULL 會誤匹配空 slot）。

安全機制：驗證模組 NID `0x3F75D4D3`；每個 hook 目標先比對 10（或 8）個位元組的簽名；三個 hook 必須全部成功，否則全部釋放並停用；影子配置失敗時只用 NULL 防護（AAC 被拒絕但不崩潰）。

## 4. 實機結果（Vita 3.65、Adrenaline 8.0.1、Default 記憶體配置，測試版 1.1）
- C75（原始 ISO）、miku3、mikuV1.2：開場影片有畫面、有聲音，速度與音高正常；mikuV1.2 原本開頭的黑畫面消失。
- 解碼 log：所有 `cmd 0x90` 皆 `ret=0`，輸出恆 4096 bytes，`fmt` 皆 `44 ac 00 00`（44100）；mikuV1.2 連續解碼 7400 frame（約 172 秒）無錯誤。
- 關鍵發現：解碼的 `a5`（fmt 區）本身就是 EDRAM 位址 `0x18B8C0`，因此 fmt 由影子區讀取，與 `cmd 0x93` 寫入的取樣率一致；先前擔心的「影子與遊戲 RAM 的 fmt 不同步」不存在。
- 輸入為原始 AAC 存取單元（非 ADTS）；第一個 frame 含 `libfaac 1.25` 的 fill 元素。
- 頭兩秒破音：測試版逐筆寫 log，前 30 次解碼每 frame 96–106 ms（即時需要 23.2 ms），之後恰為 22.7–23.4 ms。1.2 移除逐筆記錄。
- 存檔問題（C75 無法存讀檔）：PPSSPP 上症狀相同 → 遊戲本身行為，與本外掛無關。

## 5. 限制
- 僅 NID `0x3F75D4D3`（3.65–3.70）。3.60（`0x2714F07D`）、3.71（`0xEA8C1AE2`）、3.72–3.74（`0x5459B715`）的偏移不同；要支援需該韌體的 `ScePspemu` 解密檔，重新定位 `ConvertArg`、AAC 群組、崩潰函式三處。
- 只在 3.65 驗證；3.67–3.70 是依「同一模組 NID」推定相同。
- 影子區不是真正的 ME 記憶體：只涵蓋 ScePspemu 在 AAC 指令中對該位址的讀寫（8 bytes 的取樣率＋旗標）。若其他程式以不同方式使用 EDRAM 位址，行為未知（未觀察到）。
- 1.2 尚未有實機回歸紀錄（測試版 1.1 已測 C75、miku3、mikuV1.2、Outrun）。

## 6. 記錄檔
`ux0:data/adr_aacfix.log`（備援 `ur0:tai/adr_aacfix.log`），啟動時若超過 32 KB 會重建。預設只有：啟動一行、不支援／安裝失敗訊息、以及 `GUARD`／`CONV-NULL` 錯誤（各最多 20 筆）。除錯：建立空檔案 `ux0:data/adr_aacfix_debug` 可多記錄前 60 筆非解碼的 AAC 請求（不逐筆記錄解碼，避免拖慢音訊）。

## 7. 重新編譯
在 `src\` 資料夾：

```
docker run --rm -v "%cd%:/src" -w /src vitasdk/vitasdk bash -c "mkdir -p build && cd build && cmake .. && make"
```
產出 `build/adr_aacfix.suprx`。
